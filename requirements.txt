Flask>=2.0.0
# Add other dependencies as needed, e.g.:
# Flask-WTF
# Flask-SQLAlchemy
# Flask-Migrate
python-dotenv>=0.19.0

# Azure Event Hub / Kafka connectivity (used in TestConnection)
confluent-kafka>=2.0.2

# For API requests (used in banking_operations.py)
requests>=2.25.0 